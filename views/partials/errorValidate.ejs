<!-- views/partials/errorValidate.ejs -->

<% if(errors.length) { %>
  <span id="ErrorMsgs">
    Corregir errores: <br>
    <% for(var i in errors){ %>
      <div class="alert alert-warning" role="alert"> <%= errors[i].message %> </div>
    <% } %>
  </span>
<% } %>
